# CPU-Emulator
IA-32 CPU Emulator I gave up on 


This will not be updated.


## Build
In order to build, you must have the appropriate tools already installed.

Then run the following:
```bash
mkdir build
cd build
cmake ..
cmake --build .
```